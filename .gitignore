# Binaries for programs and plugins
*.exe
*.exe~
# EnableLoopback.exe is a bundled Windows runtime tool: the windows build
# installs it into the app bundle (windows/CMakeLists.txt) and the app invokes
# it at runtime (lib/views/tools.dart). It must stay tracked despite *.exe.
!windows/EnableLoopback.exe

# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/
/dist/
/android/core/.cxx/
/service/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Helper
/services/helper/target/
#libclash
/libclash/

#jniLibs
# core/src/main/jniLibs is fully generated by `setup.dart` (libclash + cgo
# headers). It must stay ignored — committing it pollutes the repo with
# 40+ MB of regenerated artefacts on every core update.
/android/core/src/main/jniLibs/

# app/src/main/jniLibs holds an embedded native relay binary, built out
# of tree and copied in. The directory is tracked so release APKs ship
# the binary even on a clean CI checkout.

# Internal planning / release / infra docs — not shipped publicly
/docs/plans/
/docs/parazitx/
/docs/release/

# Signing keys (sensitive data)
/android/app/keystore.jks
/android/app/key.properties
/android/local.properties
*.jks
*.keystore
signing.properties

# Environment variables (contains sensitive data)
.env
*.local.sh

# Playwright baseline screenshots and session data
/baseline-*.png
/.playwright-mcp/

# OpenCode local config
.opencode/
opencode.json
# Sisyphus orchestration scratch
.sisyphus/

# Serena tooling metadata
.serena/

# code-graph-mcp local code index
.code-graph/

# Android module build output (artifacts; never track)
/android/build/

# fvm (local Flutter version pin)
.fvm/
.fvmrc

# Local-only AI/agent + design tooling (kept on disk, untracked)
/AGENTS.md
/DESIGN.md

# Dev/release tooling kept local-only (build it yourself if you need it)
/tool/

# Tests kept local-only (untracked by request)
/test/
