<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required for the persistent VPN status notification on Android 13+
         (runtime request in AppPlugin.requestNotificationPermission). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Read-only network state observation for VPN reconnect handling
         (VpnPlugin.registerNetworkCallback + NET_CAPABILITY_* in NetworkRequest
         + ConnectivityManager.resolveDns). No CHANGE_NETWORK_STATE needed:
         we never mutate global network configuration; only the tunnel's own
         setUnderlyingNetworks(...) is used. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- VPN services use systemExempted FGS type to avoid the Android 14+

         dataSync 6h cumulative timeout that can crash long-running VPN
         foreground services with ForegroundServiceDidNotStopInTimeException.
         VPN apps qualify for this exemption. The matching foregroundServiceType
         is selected at runtime in BaseServiceInterface.startForeground. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <!--
        Package visibility for per-app VPN access control and config-file
        sharing is scoped narrowly via the <queries> block below; the broad
        QUERY_ALL_PACKAGES permission was removed for Google Play readiness
        (high-risk policy). Consequences are handled in code:
          * lib/views/access.dart shows a "no data" empty state when the
            installed-package list is restricted.
          * AppPlugin.getPackageIcon (PackageManager.getApplicationIcon
            lookup) and AppPlugin.isChinaPackage (PackageManager.getPackageInfo
            lookup) both wrap their package-manager calls in try/catch and
            return safe defaults when a package is not visible.
    -->

    <queries>
        <!-- Per-app VPN access control: declares a MAIN + LAUNCHER intent so
             AppPlugin.getPackages can see packages that publish a launcher
             entry under Android 11+ package-visibility rules. The actual
             visible set is whatever PackageManager returns for this query on
             the device; we make no stronger guarantee. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Deep-link / browser handoff. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <!-- AppPlugin.openFile lets the user open exported config files in a
             text viewer. The intent carries FLAG_GRANT_READ/WRITE_URI_PERMISSION
             (see intent.addFlags in AppPlugin.openFile) so the picked viewer
             receives the FileProvider URI grant automatically; in addition,
             this <intent> entry lets queryIntentActivities resolve matching
             viewers up-front so we can issue explicit grantUriPermission
             calls to each candidate before launching the intent. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <application
        android:name=".DropwebApplication"
        android:allowBackup="false"
        android:banner="@mipmap/ic_banner"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="dropweb"
        android:memtagMode="off"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="s">
        <activity
            android:name="app.dropweb.MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
            <!-- clash:// for ecosystem bots, dropweb:// for our own deep links. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="clash" android:host="install-config" />
                <data android:scheme="dropweb" android:host="install-config" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="true" />

        <!-- SECURITY: exported=false — only same-UID widgets/tile launch via explicit ComponentName. -->
        <activity
            android:name=".TempActivity"
            android:exported="false"
            android:theme="@style/TransparentTheme">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="${applicationId}.action.START" />
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="${applicationId}.action.STOP" />
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="${applicationId}.action.CHANGE" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.DropwebTileService"
            android:exported="true"
            android:icon="@drawable/ic_launcher_monochrome"
            android:label="dropweb"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            tools:targetApi="n">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
            android:value="false" />
        </service>

        <!-- SECURITY: BIND_APPWIDGET — only system AppWidget service can deliver updates. -->
        <receiver
            android:name=".widgets.DropwebWidgetProvider"
            android:exported="true"
            android:label="dropweb VPN"
            android:permission="android.permission.BIND_APPWIDGET">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/vpn_widget_info" />
        </receiver>

        <provider
            android:name=".FilesProvider"
            android:authorities="${applicationId}.files"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS"
            android:process=":background">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <service
            android:name=".services.DropwebVpnService"
            android:exported="false"
            android:foregroundServiceType="systemExempted"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

<<<<<<< HEAD
        <!-- Non-VPN proxy mode (mihomo HTTP/SOCKS without TUN). The FGS type is
             dataSync because this service relays user traffic between local
             apps and remote servers over the network. PROPERTY_SPECIAL_USE_FGS_SUBTYPE
             is intentionally NOT declared here — it is only meaningful for
             foregroundServiceType="specialUse". -->
=======
>>>>>>> 67ca011 (chore(android): remove ParazitX service declarations)
        <service
            android:name=".services.DropwebService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
