# Хендофф: итог цикла стабилизации dropweb-app, остаток и ловушки

Дата фиксации: 2026-09-08. Ветка: `dev` @ `ef5689d`.

Документ составлен как автономная точка старта для новой сессии с чистого листа. Вся информация выверена по коду, тестам и замерам на физическом устройстве Pixel 10.

---

## Раздел 1. С чего начать

1. **Код-граф проиндексирован сегодня:** проект `Users-mac-Documents-projects-dropweb-app`, 20 149 узлов, 99 441 связь. Начинать работу строго с `list_projects`, затем использовать `search_graph`, `trace_path`, `get_code_snippet`. Не повторять критическую ошибку прошлой сессии: полвечера ушло на ручной grep и слепую проверку гипотез через устройство, потому что граф не был проиндексирован, хотя `AGENTS.md` предписывает обращение к графу первым пунктом.
2. **Память:** вызывать `recall "dropweb стабилизация"`. Ключевые факты сессии:
   - `dropweb_app_stabilization_SESSION_CLOSED_2026_09_08` (сводный итог цикла);
   - `dropweb_app_V2_FIXED_VERIFIED_ON_PIXEL_2026_09_08` (корень и фикс V2);
   - `dropweb_app_WAVE3_FFI_boundary_3of4_FIXED_verified_2026_09_08` (итоги волны 3);
   - `dropweb_app_W1_device_evidence_CAPTURED_V2_rootcause_2026_09_08` (первые замеры на Pixel 10);
   - `dropweb_app_W1_checkpoint_V1_NOT_REPRODUCED_plus_FGS_storm_2026_09_08` (результаты по плитке и обнаружение FGS-шторма).
   - Причинный урок: `dropweb_app_2026_09_08_graph_unused_ghost_hunting`.
3. **Исходные документы цикла:**
   - `docs/plans/2026-09-03-stabilization-plan.md` (общий план стабилизации);
   - `docs/plans/2026-09-03-stabilization-research.md` (исследование, 31 исходная находка);
   - `docs/plans/2026-09-03-device-evidence.md` (улики и протоколы проверок на устройстве);
   - `.omo/notepads/2026-09-03-stabilization/` (рабочий блокнот сессии).

---

## Раздел 2. Состояние репозитория

- Ветка: `dev` == `origin/dev` == `ef5689d`.
- Рабочее дерево: абсолютно чистое, все изменения отправлены в удаленный репозиторий (22 коммита за цикл).
- Ветка `main` не обновлялась.
- Качество и гейты:
  - Тесты: **540 тестов** проходят успешно;
  - Анализатор: `dart analyze --fatal-warnings` возвращает код 0, ровно **615 issues** (все уровня `info`, предупреждений и ошибок 0);
  - Go-тесты: `go test` в `core/` проходит успешно;
  - Трейлеры коммитов: ровно 0 (служебные трейлеры вычищены).
- Непрерывная интеграция: файл `.github/workflows/ci.yaml` создан в рамках текущего цикла и впервые в истории проекта завершился полностью зеленым статусом (время выполнения 11m18s, 10 шагов). Файл `build.yaml` теперь ожидает завершения джобы `checks`, благодаря чему релизный пайплайн по тегу защищен от сборки на сломанных тестах.

---

## Раздел 3. Что сделано

Полностью закрыты волна 0 (3 задачи) и волна 1 (7 задач). Дополнительно закрыт дефект V2 и три задачи волны 3.

Пользовательски значимые результаты:
1. **Защита релизов тестами:** релизный пайплайн ранее не запускал ни одного теста. Теперь установлен обязательный строгий гейт в CI.
2. **Устранение утечки токена:** токен подписки утекал в открытом виде в логи приложения и в саппорт-бандлы, отправляемые пользователями в поддержку. Поведение закрыто маскированием и проверено на живом токене в 6 различных сценариях.
3. **Исправление сброса соединений при смене сети (V2):** при каждом переключении Wi-Fi↔LTE сброс DNS-пулов молча падал, оставляя туннель на нежизнеспособных адресах прежней сети. Корневая причина: событие `networkChanged` приходит в сервисный изолят, где конструктор `ClashCore._internal()` вызывал `clashInterface = clashLib!` (`lib/clash/core.dart:23`). При этом геттер `clashLib` в сервисном изоляте всегда возвращает `null` в силу взаимоисключающего контракта (`lib/clash/lib.dart:399-403`). Исправление выполнено минимальным вмешательством: класс `ClashLibHandler` получил методы `resetConnections` и `closeConnections` через существующий вызов `invokeAction`, а точка вызова выбирает нужную обертку тернарным оператором по `globalState.isService`. Проверено на Pixel 10: ошибка `[isolate-violation]` полностью исчезла, операция сброса укладывается в 10 мс.
4. **Устранение маскировки сбоев ядра под валидные данные:** мертвое ядро возвращало пустой профиль (без прокси и правил) со статусом успеха. Текст паники ядра возвращался в качестве строкового значения (метод `getCountryCode` выводил текст паники вместо двухбуквенного кода страны). Функция `asyncTestDelay` теряла параметр `url` при nil-proxy пути и записывала задержку под пустым ключом.
5. **Восстановление и герметизация тестового набора:** 25 тестовых файлов были исключены через gitignore и возвращены в репозиторий. Устранен дедлок на `path_provider` в окружении Linux: время прогона сократилось с 10 минут 24 секунд до **24 секунд**.

---

## Раздел 4. Что осталось

| Блок | Задачи | Статус и комментарий |
|---|---|---|
| Волна 2 | 2.1, 2.2a, 2.2b, 2.3, 2.4 | Пропущена решением владельца: целевые дефекты V1 и V6 не подтвердились на физическом устройстве. |
| Волна 3 | 3.1, 3.2, 3.3, 3.4 | 3.1 закрыта, 3.3 закрыта, 3.2 отклонена как спекулятивная. **Задача 3.4 не выполнена.** |
| Волна 4 | 4.1 (канал принадлежит service-engine) | Практическая цель блока (дефект V2) достигнута проще и безопаснее без перестройки владения каналом. Сама задача 4.1 не выполнялась. |
| Волна 5 | 14 задач технической гигиены | Не начата. Без явных жалоб пользователей не запускать. |
| Чекпоинты | W2, W3, W4 | Не проводились. |

---

## Раздел 5. Рекомендуемый следующий шаг: задача 3.4

Задача 3.4 представляет собой **единственный оставшийся пункт плана, который не является призраком**. В ней присутствует одновременно строгая претензия к коду и регистрируемый симптом в рантайме.

Суть дефектов по плану:
- **G3:** в файле `core/lib_android.go:143-160` присвоение `runTime = &now` выполняется **до** проверки дескриптора `if fd <= 0`. Если старт туннеля завершился неудачей, переменная `runTime` остается выставленной. Приложение считает себя подключенным при фактически отсутствующем туннеле. Это тот же класс ошибок "отказ маскируется под успех", который исправлялся в волне 3. Дефект строго доказуем чтением кода.
- **G5:** функция `quickStart` в ядре способна завершиться без ответа вообще. Требование плана: заменить `recoverGo("quickStart")` на `recoverGoFn("quickStart", ...)` с отправкой ошибки через порт, а на стороне Dart предусмотреть 30-секундный таймаут (`lib/clash/lib.dart:372-382`).
- **Связанный симптом в логах:** при запуске туннеля через QS-плитку при незапущенном основном изоляте в лог пишется `[DART] QuickStart mode - registering lazy rehandshake bridge for a later main isolate`, после чего foreground-сервис входит в цикл перезапуска с частотой примерно раз в секунду. В интервале 22:13:37-22:13:47 зафиксирован десяток повторяющихся пар `Foreground service started from background` и `FGS logic changed: WIU:DENIED`. При этом сам VPN-туннель успешно поднимается. Связь симптома с G5 пока является техническим лидом, но не доказанным выводом.
- **Критическая ловушка для разработчика:** файл `core/lib_android.go` содержит директиву `//go:build android && cgo` и при обычном запуске с `CGO_ENABLED=0` не компилируется вовсе. План требует вынести логику валидации в чистую функцию `tunStartDecision(fd int, hasConfig bool)` в файл `core/common.go` (файл компилируется без сборочных тегов) и покрыть ее unit-тестами. Проверять результат обязательно двумя командами: `CGO_ENABLED=0 go test ./...` и `make android_arm64_core`.

---

## Раздел 6. Принятая планка

В основу всех решений цикла заложен принцип:

> **Доказуемо в коде: реально, чиним. Доказуемо только поведением, которого мы не наблюдали: спекуляция, не трогаем.**

Руководствуясь этим принципом:
- Волна 2 пропущена целиком, поскольку ее мишени базировались на теоретических утверждениях о поведении, которые опровергнуты практическими проверками на стенде.
- Волна 3 реализована, так как содержала дефекты контрактов, доказуемые структурой кода.
- Волна 5 не должна инициироваться без прямых обращений пользователей или явных функциональных сбоев.

---

## Раздел 7. Призраки: не перепроверять

Примерно половина пунктов первоначального исследования оказалась ложными тревогами. Результаты измерений на Pixel 10 от 2026-09-08:
1. **V1 (залипание состояния при манипуляциях с плиткой):** не воспроизвелся 4 независимыми сценариями:
   - Одиночный тап по активной плитке: плитка штатно вызывает `handleStop`.
   - Свайп приложения из списка недавних: плитка сохраняет актуальное состояние, процесс сервиса не падает.
   - Быстрый двойной тап: второй клик физически не доходит до плитки, так как системная шторка мгновенно схлопывается.
   - Запуск приложения поверх активного туннеля, поднятого из плитки: интерфейс чисто подхватывает рабочее состояние. Ранний выход по условию `already START` в исходниках существует и логируется, но доступными пользовательскими путями не достигается.
2. **V6 (исчезновение уведомления во второй сессии):** во второй сессии после переподключения уведомление стабильно на месте (`NotificationRecord id=1 channel=dropweb FOREGROUND_SERVICE`, `isForeground=true`).
3. **V7 (пустые плашки пинга в селекторе стран):** на выборке из 242 строк лога DELAY_DIAG зафиксировано ровно 0 пустых ключей. Все 7 отображаемых плашек заполнились корректными значениями (125 / n/a / 108 / 101 / 109 / 361 / 272 ms).
4. **"Остаточная дыра" в протоколе Go:** отсутствует. Протокол честен: пустая строка `fn("")` означает успех, непустая содержит текст ошибки. Обертка `recoverGoFn` при возникновении паники транслирует ее в этот контракт корректно. Все вызывающие функции (`updateGeoData`, `updateExternalProvider`, `sideLoadExternalProvider`) обрабатывают возвращаемый результат.
5. **Игнорирование результата в `changeProxy` (`lib/controller.dart:1040`):** единственный участок без проверки возвращаемого значения. Анализ графа показал ровно одного потребителя (`changeProxyDebounce` с задержкой). Реального вреда для стабильности нет. Вопрос закрыт как несущественный.

---

## Раздел 8. Ловушки окружения

Все пункты проверены на личном опыте и привели к потере времени в прошлых сессиях:

1. **Несовпадение номеров строк в документации:** номера строк в `2026-09-03-stabilization-plan.md` и `research.md` сняты с другой ветки (`f68f7fd`, верхушка `feat/meowzic` = `main`+27). Все поиски в кодовой базе вести строго по именам символов и сигнатурам, ни в коем случае не по абсолютным номерам строк.
2. **Версионирование Flutter/Dart:** использовать строго `fvm flutter` и `fvm dart`. Системный Homebrew-пакет устанавливает версию 3.44, которая ломает сборку проекта. Файл `.fvmrc` жестко фиксирует поддерживаемую версию 3.41.6.
3. **Требование к версии Java:** для локального Gradle обязателен параметр `JAVA_HOME=/opt/homebrew/opt/openjdk@17`. Стандартный JDK из Homebrew имеет версию 26, несовместимую с текущей конфигурацией AGP.
4. **Сборочные теги Go:** исходник `core/lib.go` помечен тегом `//go:build cgo`. Команда `CGO_ENABLED=0 go vet ./...` его молча пропускает. Единственная достоверная валидация изменений ядра: вызов `make android_arm64_core` (занимает порядка 30 секунд). Напротив, `core/server.go` скомпилирован под `//go:build !cgo`. Ни одна отдельная команда не проверяет оба файла одновременно.
5. **Несуществующий сервис уведомлений в Android:** вызов `dumpsys notification_manager` возвращает пустой вывод, так как такого сервиса в системе нет. Корректная команда проверки: `dumpsys notification --noredact`. Ошибка в имени команды едва не привела к ложному подтверждению бага V6.
6. **Поведение `rtk grep` на пайпах:** утилита `rtk grep` опасна внутри конвейеров команд. Не обнаружив совпадений в стандартном вводе (stdin), она начинает рекурсивный поиск по файлам репозитория, засоряя вывод фрагментами служебных документов. На пайпах применять стандартный системный `grep`.
7. **Опасность слепых тапов через `adb shell input tap`:** отправка `KEYCODE_BACK` свернула окно приложения, в результате чего последующие два клика ушли в активный Telegram владельца. Перед каждым программным кликом обязательно проверять `topResumedActivity`. Учитывать, что открытая шторка уведомлений является системным оверлеем, который не меняет статус resumed activity. Шторку требуется закрывать принудительно через `cmd statusbar collapse`.
8. **Параллельная установка сборок и конфликт VPN-интерфейса:** отладочная сборка устанавливается независимо от боевой благодаря суффиксу идентификатора (`applicationIdSuffix .debug`, пакет `app.dropweb.debug`) и не повреждает боевые профили. Однако операционная система Android поддерживает строго один активный туннель: запуск отладочного VPN разрывает рабочее соединение пользователя.
9. **Ручное добавление плитки на Android 16:** для отладочной плитки на экране 1080×2424 физические координаты составляют (158, 1361). Команда `settings put secure sysui_qs_tiles` на Android 16 игнорируется, поскольку хранилище SystemUI изолировано. Добавить плитку можно только вручную через редактирование шторки.
10. **Безопасность системных компонентов:** активность `TempActivity` намеренно сконфигурирована с `exported="false"`, а виджет защищен разрешением `BIND_APPWIDGET`. Программные вызовы `GlobalState.handleStart()` извне невозможны. Запрещено ослаблять манифест ради упрощения автотестов.
11. **Служебные трейлеры агентов в коммитах:** автоматическая обвязка агентов пытается вставлять в коммиты рекламные строки вида `Ultraworked with [Sisyphus]` и `Co-authored-by: Sisyphus <clio-agent@sisyphuslabs.ai>`. Репозиторий является публичным. Ранее 4 коммита пришлось экстренно переписывать через `filter-branch`. В каждое задание исполнителю необходимо явно включать запрет на добавление любых трейлеров.

---

## Раздел 9. Открытые лиды

Неоформленные зацепки, требующие внимания при появлении симптомов:
1. **FGS-шторм:** частый перезапуск службы при старте с плитки без главного изолята (подробности в разделе 5, вероятная взаимосвязь с задачей G5).
2. **Раскол ключа в карте задержек:** карта задержек использует композитный ключ `(testUrl, name)`. Узел регистрируется одновременно под двумя разными URL: под адресом группы (`urlFrom=cardState`, например `cp.cloudflare.com`) и под глобальным фолбэком (`declaredUrl=<null>`, `urlFrom=fallback`, `www.gstatic.com`). В текущей конфигурации поведение безопасно, так как чтение и запись дублируются. Однако ситуация приведет к пустому бейджу, если одна из сторон не получит значение.
3. **Непроверенные сценарии A4 и A5:** сценарии A4 (старт с плитки после холодной перезагрузки устройства) и A5 (поведение плитки при включенном режиме полета) не исследованы. Для них требуется перезагрузка физического смартфона и переключение авиарежима, недоступное через adb без root-прав на Android 16.
4. **Устаревшие версии GitHub Actions:** шаги `checkout@v4`, `setup-go@v5`, `setup-java@v4` принудительно переведены на рантайм Node 24, при этом экшен `setup-java@v4` официально объявлен устаревшим.
5. **Несовпадение версии Flutter для macOS:** пайплайн `build-macos.yaml` фиксирует Flutter 3.32.8, тогда как корневой `.fvmrc` требует 3.41.6. Расхождение существует в релизных скриптах, в задачи стабилизации не входило.

---

## Раздел 10. Путь к пре-релизу

Текущая версия в коде: `0.8.7+2050000014`, крайний релизный тег: `v0.8.7`. Следующий релизный шаг: **`0.8.8-pre.1`**.

Обязательный регламент выпуска:
1. Повысить номер версии в `pubspec.yaml` отдельным коммитом.
2. **Подготовить описание релиза `.github/release_notes/v0.8.8-pre.1.md`:** составить содержательные разделы на русском языке в тональности бренда. Если файл отсутствует, CI опубликует сырой автоматический список коммитов, что является недопустимым снижением планки качества. Текст обязательно согласовать с владельцем перед установкой тега.
3. **Установка тега выполняется исключительно владельцем.** После пуша тега CI автоматически выполнит сборку, сформирует GitHub Release и отправит публикацию в Telegram-канал (пре-релизы направляются в топик 518).

Три ключевых тезиса для заметок к релизу:
- Переключение между мобильной сетью и Wi-Fi больше не нарушает защиту и не обрывает туннель.
- Токен подписки надежно скрыт и не попадает в диагностические отчеты.
- Сбои и ошибки сетевого ядра выводятся честно и больше не маскируются под успешное подключение.

---

## Раздел 11. Ошибки прошлой сессии

Анализ просчетов для предотвращения их повторения:
1. **Игнорирование базы знаний и графа кода:** граф репозитория не был проиндексирован, а статус индекса не был проверен в начале работы вопреки прямому требованию регламента `AGENTS.md`. В результате половина рабочего времени ушла на неэффективный grep и дорогую проверку ложных гипотез через физическое устройство (сборка, установка APK, ручное воспроизведение). Владелец заметил неэффективность раньше исполнителя.
2. **Поспешные заявления и самоопровержения:**
   - Преждевременное утверждение о том, что документ research ошибся в описании V2, тогда как исходный анализ оказался абсолютно точным.
   - Замена диагностического поля `clashLibReady` на `clashLibHandler`, что заменило одну тавтологию другой и привело к выводу ложного значения `true` на ветке сбоя.
   - Ошибочный пересказ владельцу несуществующей "остаточной дыры" в протоколе Go-ядра с последующей потерей времени на опровержение.
3. **Искажение спецификации при передаче задачи:** исполнителю была передана собственная вольная формулировка четвертой задачи волны 3 вместо канонического текста плана. В итоге задача 3.4 выпала из реализации и обнаружилась только на финальной сверке чек-листа.
4. **Избыточная фиксация на дефекте V1:** после двух безуспешных попыток воспроизведения расследование следовало немедленно остановить. Вместо этого было выполнено четыре захода с разворачиванием отдельной ветки поиска, отнявшей существенный ресурс сессии.
