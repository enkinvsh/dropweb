# ParazitX archive note

ParazitX was physically removed from Play-facing app code. Historical docs in this directory are context only and do not describe shipped Play-facing behavior.

The preservation branch is `wip/parazitx-quarantine-2026-05-17`.

Cabinet access remains browser-only through `openCabinetBrowser(Uri)`.
